---
id: 133
name: "Open Record/Request"
category: Records
status: draft
input_patterns:
  - "Open Record/Request [ ... ]"
fm_name: "Open Record/Request"
xml:
  step_name: "Open Record/Request"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Open Record/Request

## Mapping rules

- `name="Open Record/Request"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="133"` for all `Open Record/Request` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="133" name="Open Record/Request">
        </Step>

```
