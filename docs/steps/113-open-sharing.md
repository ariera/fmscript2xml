---
id: 113
name: "Open Sharing"
category: Open Menu Item
status: draft
input_patterns:
  - "Open Sharing [ ... ]"
fm_name: "Open Sharing"
xml:
  step_name: "Open Sharing"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Open Sharing

## Mapping rules

- `name="Open Sharing"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="113"` for all `Open Sharing` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="113" name="Open Sharing">
        </Step>

```
