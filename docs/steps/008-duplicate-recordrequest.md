---
id: 8
name: "Duplicate Record/Request"
category: Records
status: draft
input_patterns:
  - "Duplicate Record/Request [ ... ]"
fm_name: "Duplicate Record/Request"
xml:
  step_name: "Duplicate Record/Request"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Duplicate Record/Request

## Mapping rules

- `name="Duplicate Record/Request"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="8"` for all `Duplicate Record/Request` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="8" name="Duplicate Record/Request">
        </Step>

```
