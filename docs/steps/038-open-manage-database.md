---
id: 38
name: "Open Manage Database"
category: Open Menu Item
status: draft
input_patterns:
  - "Open Manage Database [ ... ]"
fm_name: "Open Manage Database"
xml:
  step_name: "Open Manage Database"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Open Manage Database

## Mapping rules

- `name="Open Manage Database"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="38"` for all `Open Manage Database` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="38" name="Open Manage Database">
        </Step>

```
