---
id: 101
name: "Copy Record/Request"
category: Records
status: draft
input_patterns:
  - "Copy Record/Request [ ... ]"
fm_name: "Copy Record/Request"
xml:
  step_name: "Copy Record/Request"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Copy Record/Request

## Mapping rules

- `name="Copy Record/Request"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="101"` for all `Copy Record/Request` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="101" name="Copy Record/Request">
        </Step>

```
