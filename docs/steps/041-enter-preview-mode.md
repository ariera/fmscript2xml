---
id: 41
name: "Enter Preview Mode"
category: Navigation
status: draft
input_patterns:
  - "Enter Preview Mode [ ... ]"
fm_name: "Enter Preview Mode"
xml:
  step_name: "Enter Preview Mode"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Enter Preview Mode

## Mapping rules

- `name="Enter Preview Mode"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="41"` for all `Enter Preview Mode` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="41" name="Enter Preview Mode">
        </Step>

```
