---
id: 104
name: "Delete Portal Row"
category: Records
status: draft
input_patterns:
  - "Delete Portal Row [ ... ]"
fm_name: "Delete Portal Row"
xml:
  step_name: "Delete Portal Row"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Delete Portal Row

## Mapping rules

- `name="Delete Portal Row"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="104"` for all `Delete Portal Row` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="104" name="Delete Portal Row">
  
          
  <NoInteract state="True"/>
  
        
</Step>

```
