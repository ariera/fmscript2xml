---
id: 172
name: "Upload to FileMaker Server"
category: Open Menu Item
status: draft
input_patterns:
  - "Upload to FileMaker Server [ ... ]"
fm_name: "Upload to FileMaker Server"
xml:
  step_name: "Upload to FileMaker Server"
  enable_default: True
  wrapper: step-only
---

## Description

FileMaker script step: Upload to FileMaker Server

## Mapping rules

- `name="Upload to FileMaker Server"` in the `<Step>` element.
- `enable="True"` unless the step is explicitly disabled in the text.
- `id="172"` for all `Upload to FileMaker Server` steps.

## Examples

### Output (XML, step-only)

```xml
<Step enable="True" id="172" name="Upload to FileMaker Server">
        </Step>

```
